spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestor_economico?useSSL=false&serverTimezone=UTC
    username: naveira
    password: naveira
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
